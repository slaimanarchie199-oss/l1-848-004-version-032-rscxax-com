(function(){var wrap=document.querySelector('[data-player]');if(!wrap)return;var video=wrap.querySelector('video');var overlay=wrap.querySelector('.play-overlay');var url=wrap.getAttribute('data-stream');var ready=false;function attach(){if(ready)return;if(video.canPlayType('application/vnd.apple.mpegurl')){video.src=url}else if(window.Hls&&window.Hls.isSupported()){var hls=new window.Hls({enableWorker:true});hls.loadSource(url);hls.attachMedia(video)}else{video.src=url}ready=true}function start(){attach();if(overlay)overlay.classList.add('hidden');video.controls=true;var p=video.play();if(p&&p.catch)p.catch(function(){})}if(overlay)overlay.addEventListener('click',start);video.addEventListener('click',function(){if(video.paused)start()})})();